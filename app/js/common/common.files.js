define([
    'common/common.views',
    'common/common.collections',
    'common/common.models',
], function (views, collections, models) {
    return {
        views: views,
        collections: collections,
        models: models
    }
});